# Python
__pycache__/
*.py[cod]
*.egg-info/

# Environments
venv/
.venv/
.env

# Carries the real DB password; only the .example is committed.
scripts/init_db.sql

# Django
db.sqlite3
db.sqlite3-journal
staticfiles/
media/
*.log

# The portal's compiled front-end: ~43MB of `flutter build web` output, rebuilt
# from the Flutter project by its scripts/build-portal.ps1. The views and the
# routes that serve it are committed; the build artefact is not.
portal/web/

# Editors
.idea/
.vscode/

# Filled-in deployment config: contains the real hostname and cert paths.
deploy/nginx.conf
deploy/*.service
deploy/certbot/
